export const Header = () => {
  return (
    <header
      style={{
        background: '#000',
        color: "#fff",
        margin: 0,
        padding: "4px",
      }}
    >
      <h1>header</h1>
    </header>
  );
};
