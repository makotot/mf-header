import React from 'react'

export const Header = () => {
  return (
    <header
      style={{
        background: '#222',
        color: "#fff",
        margin: 0,
        padding: "16px",
      }}
    >
      <h1>header</h1>
    </header>
  );
};
